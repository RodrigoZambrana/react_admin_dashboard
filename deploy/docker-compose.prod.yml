x-backend-common: &backend-common
  restart: unless-stopped
  env_file:
    - ./env/backend.prod.env
  environment:
    NODE_ENV: production
    PORT: 3000
    PRISMA_APPLY_MIGRATIONS: ${PRISMA_APPLY_MIGRATIONS:-false}

x-frontend-common: &frontend-common
  restart: unless-stopped
  env_file:
    - ./env/frontend.prod.env

x-storefront-common: &storefront-common
  restart: unless-stopped
  env_file:
    - ./env/storefront.prod.env
  environment:
    NODE_ENV: production
    PORT: 3000

services:
  frontend:
    <<: *frontend-common
    build:
      context: ..
      dockerfile: frontend/Dockerfile
      # Override VITE_CLIENT_SLUG cuando necesites construir una variante específica (e.g. urucortinas)
      args:
        VITE_CLIENT_SLUG: ${VITE_CLIENT_SLUG:-core}
    ports:
      - "8080:80"
    environment:
      SERVER_NAME: ${APP_DOMAIN_PROD:-localhost}
      BACKEND_HOST: backend
      BACKEND_PORT: 3000
  backend:
    <<: *backend-common
    build:
      context: ../backend
      dockerfile: Dockerfile
      # Override CLIENT_SLUG para apuntar a una variante específica durante la build (e.g. urucortinas)
      args:
        CLIENT_SLUG: ${CLIENT_SLUG:-core}
    ports:
      - "4000:3000"
  storefront:
    <<: *storefront-common
    build:
      context: ..
      dockerfile: ecommerce/Dockerfile
      target: runtime
    ports:
      - "${STOREFRONT_HTTP_PORT:-3000}:3000"
    depends_on:
      - backend
    labels:
      - "traefik.enable=${TRAEFIK_ENABLE_STOREFRONT:-false}"
      - "traefik.http.routers.storefront.rule=Host(`${TRAEFIK_STOREFRONT_HOST:-storefront.local}`)"
      - "traefik.http.routers.storefront.entrypoints=${TRAEFIK_ENTRYPOINT:-web}"
      - "traefik.http.services.storefront.loadbalancer.server.port=3000"
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://127.0.0.1:3000/api/health || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3

  db:
    image: postgres:16-alpine
    restart: unless-stopped
    profiles:
      - managed-db
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_DB: ${POSTGRES_DB:-dashboard}
    volumes:
      - postgres_data_prod:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data_prod:
